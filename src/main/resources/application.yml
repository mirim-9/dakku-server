
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/dakku?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 1234

  jpa:
    show_sql: true 
    hibernate:
      ddl-auto: update

logging:
  level:
    org.hibernate:
      type.descriptor.sql: trace #show parameter binding
      SQL: DEBUG